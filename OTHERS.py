
# alphabet = string.ascii_lowercase
# for letter in alphabet:
#     if pygame.key.name(event.key) == letter:
#         print(letter)
#     if event.key == K_SHIFT 
#         if pygame.key.name(event.key) == letter:
#               print(letter.upper())



# elif self.bullet_hit_edge and (self.rect.x <= 30 or self.rect.x >= VARIABLE.WIDTH - 50):
#     self.xvel = -self.xvel
#     self.image = pygame.transform.rotozoom(WEAPON.N_BULLET,self.deg,0.4)
# elif self.bullet_hit_edge and  (self.rect.y <= 30 or self.rect.y >= VARIABLE.HEIGHT - 50):
#     self.yvel = -self.yvel
#     self.image = pygame.transform.rotozoom(WEAPON.N_BULLET,self.deg,0.4)

# if self.bullet_hit_edge:
#     self.rect.x +=  self.xvel
#     self.rect.y -=  self.yvel            






#     elif player.player.right >= block.rect.left + 5:
#         player.vel = 0
#     elif player.player.left <= block.rect.right - 5:
#         player.vel = 0
# player.vel = 10